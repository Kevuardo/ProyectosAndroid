<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">

    <!-- Agregar contacto. -->

    <!-- Ajustes de la agenda. -->
    <item
        android:id="@+id/action_ajustes"
        android:icon="@drawable/ic_more_vert_black_48dp"
        android:title="Ajustes"
        app:showAsAction="always">

        <!-- Submenú de ajustes. -->
        <menu>
            <item
                android:id="@+id/action_info"
                android:title="Información de la app"
                android:icon="@drawable/ic_info_outline_black_48dp"
                app:showAsAction="ifRoom" />
            <item
                android:id="@+id/action_vaciar"
                android:title="Vaciar lista de contactos"
                android:icon="@drawable/ic_delete_forever_black_48dp"
                app:showAsAction="ifRoom" />
            <item
                android:id="@+id/action_extra"
                android:title="Extras de la aplicación"
                android:icon="@drawable/ic_new_releases_black_48dp"
                app:showAsAction="ifRoom" />
        </menu>

    </item>

</menu>